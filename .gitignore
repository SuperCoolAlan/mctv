# Environment variables (contains secrets)
.env
setup/.env

# Cloudflare tunnel credentials and tokens
k0s/cloudflare-tunnel/tunnel-credentials.yaml
# Removed - now using SOPS encryption
k0s/cloudflare-tunnel/tunnel-credentials-configmap.yaml
# Removed - now using SOPS encryption
# Removed - now using SOPS encryption

# Kubernetes configuration
kubeconfig.yaml
kubeconfig
setup/kubeconfig.yaml
setup/kubeconfig
setup/cluster.yaml
setup/cluster-ip.yaml

# Backups
backups/
setup/backups/
*.tar.gz
validation-report-*.txt
setup/validation-report-*.txt

# k0s backups
k0s-backup-*.tar.gz
setup/k0s-backup-*.tar.gz

# Recovery documentation (may contain sensitive info)
RECOVERY.md
setup/RECOVERY.md

# Temporary files
*.tmp
*.backup
*.bak
*~

# SSH keys (if accidentally placed here)
*.pem
*.key
id_rsa*
momscloset*
setup/*.pem
setup/*.key
setup/id_rsa*
setup/momscloset*

# OS files
.DS_Store
Thumbs.db

# Helm charts (downloaded during kustomize build)
k0s/*/charts/
charts/

# Large backup/image files
*.dmg
*.dd
*.img
*.gz
*.iso
