apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cloudflare

# Create the namespace, config, and deployment
resources:
  - namespace.yaml
  - tunnel-config.yaml
  # Note: tunnel-credentials-configmap.yaml must be created from template before deployment
  - cloudflared-deployment.yaml