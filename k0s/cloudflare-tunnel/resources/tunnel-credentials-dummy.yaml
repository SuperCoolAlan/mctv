apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudflared-credentials
  namespace: cloudflare
data:
  # Dummy file - actual authentication is done via tunnel token
  credentials.json: |
    {
      "Note": "This deployment uses token-based authentication. See tunnel-token secret."
    }