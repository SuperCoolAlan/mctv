apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: frigate-secret-generator
  annotations:
    config.kubernetes.io/function: |
        exec:
          path: ksops
files:
  - secrets/camera-credentials.enc.yaml
  - ../shared-secrets/docker-registry-secret.enc.yaml